apiVersion: v1
kind: Service
metadata:
  name: {{include "joyna.frontendServiceName" .}}
  namespace: {{.Values.namespace}}
  labels: {{- include "joyna.selectorLabels" . | nindent 4}}
spec:
  selector:
    app: frontend
  ports:
    - port: {{.Values.frontend.port}}
      name: frontend
